# 同声传译系统工作流程分析

## 系统架构图

```
┌─────────────┐         ┌──────────────┐         ┌──────────────┐         ┌─────────────┐
│   浏览器    │         │  后端服务器   │         │  火山引擎API  │         │   浏览器    │
│  (前端)     │         │  (中继服务器) │         │  (翻译服务)   │         │  (前端)     │
└──────┬──────┘         └──────┬───────┘         └──────┬───────┘         └──────┬──────┘
       │                      │                        │                        │
       │  1. 音频采集         │                        │                        │
       │  (麦克风 → PCM)      │                        │                        │
       │                      │                        │                        │
       │  2. WebSocket连接    │                        │                        │
       ├─────────────────────>│                        │                        │
       │  (ws://localhost:15677/ws)│                    │                        │
       │                      │                        │                        │
       │  3. 发送音频数据     │                        │                        │
       ├─────────────────────>│                        │                        │
       │  (二进制PCM数据)     │                        │                        │
       │                      │                        │                        │
       │                      │  4. 连接火山引擎       │                        │
       │                      ├───────────────────────>│                        │
       │                      │  (wss://openspeech...)│                        │
       │                      │                        │                        │
       │                      │  5. 发送StartSession   │                        │
       │                      ├───────────────────────>│                        │
       │                      │  (protobuf)           │                        │
       │                      │                        │                        │
       │                      │  6. 接收SessionStarted │                        │
       │                      │<───────────────────────┤                        │
       │                      │  (event=150)          │                        │
       │                      │                        │                        │
       │                      │  7. 转发音频数据       │                        │
       │                      ├───────────────────────>│                        │
       │                      │  (TaskRequest + PCM)   │                        │
       │                      │                        │                        │
       │                      │  8. 接收翻译结果       │                        │
       │                      │<───────────────────────┤                        │
       │                      │  (原文/译文/TTS)       │                        │
       │                      │                        │                        │
       │  9. 接收翻译结果     │                        │                        │
       │<─────────────────────┤                        │                        │
       │  (JSON格式)          │                        │                        │
       │                      │                        │                        │
       │  10. 显示原文/译文   │                        │                        │
       │  11. 播放TTS音频     │                        │                        │
       │                      │                        │                        │
```

## 详细工作流程

### 阶段 1: 初始化连接

**步骤 1.1: 前端启动**
- 用户点击"开始翻译"按钮
- 前端调用 `start()` 方法

**步骤 1.2: 建立 WebSocket 连接**
- 前端连接到 `ws://localhost:15677/ws` (或 `wss://` 如果使用 HTTPS)
- 使用 aiohttp 在同一端口提供 HTTP 和 WebSocket 服务
- 后端 `handle_client()` 被调用

**步骤 1.3: 后端连接火山引擎**
- 后端创建 `VolcengineASTClient` 实例
- 连接到 `wss://openspeech.bytedance.com/api/v4/ast/v2/translate`
- 发送认证头：`X-Api-App-Key`, `X-Api-Access-Key`, `X-Api-Resource-Id`, `X-Api-Connect-Id`

**步骤 1.4: 启动会话**
- 后端发送 `StartSession` 请求（protobuf 格式）
- 包含配置：源语言、目标语言、音频格式等
- 火山引擎返回 `SessionStarted` (event=150)
- 后端保存 `session_id`

### 阶段 2: 音频采集和发送

**步骤 2.1: 前端音频采集**
- 获取麦克风权限：`navigator.mediaDevices.getUserMedia()`
- 创建 `AudioContext` (16kHz)
- 使用 `ScriptProcessorNode` 处理音频
- 每 80ms (约 1280 样本) 触发一次 `onaudioprocess`

**步骤 2.2: 音频格式转换**
- 输入：Float32Array (32位浮点数，范围 -1.0 到 1.0)
- 转换：`convertFloat32ToPCM16()` 
- 输出：ArrayBuffer (16位整数，小端序)

**步骤 2.3: 音量检测**
- 计算音频数据的平均绝对值
- 如果音量 < 0.001%，跳过（静音检测）
- 否则发送到服务器

**步骤 2.4: 发送到后端**
- 通过 WebSocket 发送二进制 PCM 数据
- 后端 `_handle_client_message()` 接收

### 阶段 3: 后端转发

**步骤 3.1: 后端接收音频**
- 检查会话是否已启动
- 如果未启动，自动发送 `StartSession`

**步骤 3.2: 构建 protobuf 请求**
- 创建 `TranslateRequest` 消息
- 设置 `event = Type.TaskRequest` (200)
- 设置 `session_id`
- 将 PCM 数据放入 `source_audio.binary_data`

**步骤 3.3: 发送到火山引擎**
- 序列化 protobuf 消息
- 通过 WebSocket 发送到火山引擎

### 阶段 4: 火山引擎处理

**步骤 4.1: 语音识别 (ASR)**
- 识别音频中的语音
- 生成原文文本

**步骤 4.2: 机器翻译 (MT)**
- 将原文翻译成目标语言
- 生成译文文本

**步骤 4.3: 语音合成 (TTS)**
- 将译文转换为语音
- 生成 ogg_opus 格式的音频

### 阶段 5: 接收和显示结果

**步骤 5.1: 后端接收响应**
- `_receive_messages()` 循环接收消息
- 解析 protobuf 响应
- 转换为 JSON 格式

**步骤 5.2: 事件类型处理**
- `SourceSubtitleResponse` (1419): 原文片段
- `SourceSubtitleEnd` (1420): 原文句子结束
- `TranslationSubtitleResponse` (1422): 译文片段
- `TranslationSubtitleEnd` (1423): 译文句子结束
- `TTSResponse` (480): TTS 音频数据

**步骤 5.3: 转发到前端**
- 后端通过 WebSocket 发送 JSON 消息
- 前端 `handleWebSocketMessage()` 接收

**步骤 5.4: 前端显示和播放**
- 更新原文显示区域
- 更新译文显示区域
- 播放 TTS 音频（如果支持 ogg_opus 解码）

## 关键检查点

### 检查点 1: 前端音频采集
- ✅ 麦克风权限已授予
- ✅ AudioContext 创建成功
- ✅ ScriptProcessorNode 正常工作
- ✅ 音量检测通过（> 0.001%）
- ✅ PCM 数据正确转换

### 检查点 2: WebSocket 连接
- ✅ 前端连接到后端成功
- ✅ 后端 WebSocket 服务器运行
- ✅ 消息可以双向传输

### 检查点 3: 后端到火山引擎
- ✅ 后端连接到火山引擎成功
- ✅ StartSession 请求已发送
- ✅ 收到 SessionStarted 响应
- ✅ session_id 已保存

### 检查点 4: 音频数据流
- ✅ 前端发送音频数据
- ✅ 后端接收音频数据
- ✅ 后端转发到火山引擎
- ✅ 火山引擎接收音频数据

### 检查点 5: 翻译结果
- ✅ 火山引擎返回响应
- ✅ 后端解析 protobuf
- ✅ 后端转发到前端
- ✅ 前端显示结果

## 常见问题排查

### 问题 1: 没有收到翻译结果
**可能原因：**
- 音频数据未发送到服务器
- 会话未启动
- 火山引擎未处理音频

**检查方法：**
1. 查看浏览器控制台：是否有"开始发送音频数据"日志
2. 查看后端日志：是否收到音频包
3. 查看后端日志：是否收到 SessionStarted

### 问题 2: 收到原文但没有译文
**可能原因：**
- 翻译服务问题
- 目标语言配置错误

**检查方法：**
1. 查看后端日志：是否收到 TranslationSubtitleResponse
2. 检查配置：target_language 是否正确

### 问题 3: 音频发送但无响应
**可能原因：**
- 音频格式不正确
- 采样率不匹配
- 会话超时

**检查方法：**
1. 检查音频格式：16kHz, 16bit, 单通道
2. 检查会话状态：是否收到 SessionStarted
3. 查看火山引擎错误响应

## 调试建议

1. **启用详细日志**：检查浏览器控制台和后端日志
2. **逐步测试**：先测试麦克风，再测试连接，最后测试翻译
3. **检查网络**：确保能访问火山引擎 API
4. **验证配置**：检查 API 凭证和配置是否正确

